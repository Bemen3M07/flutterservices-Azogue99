name: empty
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.5 # Afegim el paquet HTTP per poder fer requests.
  flutter_dotenv: ^5.2.1 # Afegim el paquet flutter_dotenv per poder llegir variables d'entorn (.env).

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
